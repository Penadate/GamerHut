@model UpdateCategoryModel

<form method="post" asp-action="Edit" asp-route-gameId="@ViewData["GameId"]" asp-route-id="@ViewData["Id"]" asp-route-operation="@ViewData["operation"]">
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">Edit Category</h2>
            <hr />
        </div>

        <div asp-validation-summary="All"></div>

        <div class="mb-3">
            <label asp-for="Keyword"></label>
            <input asp-for="Keyword" class="form-control" />
        </div>

        <div class="mb-3">
            <label asp-for="Name"></label>
            <input asp-for="Name" class="form-control" />
        </div>
        <button type="submit" class="btn btn-primary" style="width:150px">Update</button>
        <a class="btn btn-secondary" asp-controller="Category" asp-action="Index" asp-route-gameId="@ViewData["GameId"]" asp-route-operation="@ViewData["operation"]" style="width:150px">Back to List</a>>
    </div>
</form>

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}